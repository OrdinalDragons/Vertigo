# =============================================================================
# VERCEL DEPLOYMENT CONFIGURATION
# =============================================================================
# This file documents all environment variables required for the application.
# For Vercel deployment:
# 1. Add these variables to your Vercel project: Settings > Environment Variables
# 2. Configure variables for Production, Preview, and Development environments
# 3. Redeploy after adding/updating environment variables
#
# Database: Use Neon (recommended), Supabase, or Vercel Postgres for serverless compatibility
# Backend: This app includes an Express server that requires separate hosting (not compatible with Vercel serverless)
# =============================================================================

# Database Configuration
# Required: PostgreSQL connection string
# For Vercel: Use Neon (https://neon.tech) or Vercel Postgres for optimal serverless performance
# The @neondatabase/serverless driver is already included for connection pooling
DATABASE_URL=postgresql://user:password@host:port/database

# Session Secret
# Required: Secret key for session encryption
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here

# Admin Configuration
# Required: Comma-separated list of Solana wallet addresses with admin privileges
ADMIN_WALLETS=AdminWallet1111111111111111111111111111,AdminWallet2222222222222222222222222222

# Privy Configuration (TODO - for production)
# PRIVY_APP_ID=your-privy-app-id
# PRIVY_APP_SECRET=your-privy-app-secret

# Solana Configuration
# Required: Environment setting - devnet (for testing) or mainnet (for production)
SOLANA_ENV=devnet

# Solana RPC URL (Optional - overrides default RPC endpoints)
# If not set, defaults to:
#   - Devnet: https://api.devnet.solana.com
#   - Mainnet: Helius RPC (requires SOLANA_HELIUS_API_KEY)
# SOLANA_RPC_URL=https://api.devnet.solana.com

# Helius API Key (Required for mainnet)
# Get your free API key from https://www.helius.dev/
# SOLANA_HELIUS_API_KEY=your-helius-api-key-here

# For mainnet deployment, uncomment these:
# SOLANA_ENV=mainnet
# SOLANA_HELIUS_API_KEY=your-helius-api-key-here

# Metaplex Admin Configuration
# Required: Path to the admin keypair JSON file for minting NFTs
# Generate a new keypair with: solana-keygen new --outfile ./secrets/metaplex-admin.json
# IMPORTANT: Fund this wallet with SOL on devnet/mainnet before minting NFTs
# - NFT minting + storage costs approximately 0.01-0.02 SOL per mint
# - Storage costs for images/metadata: ~0.0001-0.001 SOL per file
# Devnet airdrop: solana airdrop 2 <PUBKEY> --url devnet
# For Vercel: Store this as a base64-encoded string in environment variables, or use Vercel Secrets
METAPLEX_ADMIN_KEYPAIR_PATH=./secrets/metaplex-admin.json

# Port Configuration
# Default: 5000 (used by Express server)
# Note: Vercel ignores this for serverless functions
PORT=5000

# Node Environment
# Automatically set by Vercel (production/preview/development)
NODE_ENV=development
